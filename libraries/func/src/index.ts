/**
 * @deprecated Moved to `@rhombus-toolkit/types`. This package is a pure
 * re-export shim and will not be developed further — the types below are the
 * same declarations, re-exported from their new home.
 */
export type { AbstractCtor, Action, AsyncAction, AsyncFunc, AsyncSub, Ctor, Func, Sub } from '@rhombus-toolkit/types';
