{
  "//": "lib: [\"ES2019\"] is the floor this package actually needs, and one identifier sets it. ES2019 comes from Object.fromEntries (obj.ts, and flattenMap.ts through it) -- verified ES2018 and ES2017 fail on exactly that one (TS2550, 'try changing lib to es2019 or later'), and ES2015 additionally fails on Object.values/Object.entries (ES2017). The ESNext.Iterator sliver this file used to carry left with the iterable helpers, now @rhombus-toolkit/iterable: Iterator.from was the only identifier that needed it, and nothing remaining here names Iterator, Iterable or Symbol.iterator at all -- verified ES2019 on its own now passes. Nothing else here reaches past lib.es5; the two workspace dependencies clear this floor themselves -- @rhombus-toolkit/types is at ES2018 and @rhombus-toolkit/type-guards, which supplies isFunction, is too. No DOM: nothing here names a DOM identifier. types: [] is inherited from tsconfig.lib.json and stays -- the runtime spec below is excluded rather than given @types/bun, so no ambient package can quietly loosen this floor. exclude: the *.spec.ts runtime tests belong to `bun test`, not to the library program.",
  "extends": "../../tsconfig.lib.json",
  "compilerOptions": {
    "lib": ["ES2019"]
  },
  "include": ["src/**/*"],
  "exclude": ["src/**/*.spec.ts"]
}
